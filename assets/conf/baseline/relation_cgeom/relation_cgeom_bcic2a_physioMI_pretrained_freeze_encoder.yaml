# Relation-CGeom + BCIC2a with PhysioMI pretrained encoder frozen.
# This trains only the classifier head on top of the pretrained encoder.
seed: 42
master_port: 51643
multitask: false
model_type: relation_cgeom
fs: 256

data:
  batch_size: 32
  num_workers: 4
  datasets:
    bcic_2a: finetune

model:
  pretrained_path: assets/run/pretrain/relation_cgeom/motor_mv_img_pretrain_bci/local_260414161330/relation_cgeom_encoder_best.pt
  output_dims: 320
  hidden_dims: 64
  depth: 7
  bands:
    - [8.0, 13.0]
    - [13.0, 20.0]
    - [20.0, 30.0]
  use_raw_branch: true
  use_complex_branch: true
  use_hyperbolic_branch: true
  share_band_encoder: false
  band_fusion_type: concat_linear
  hyperbolic_depth: 1
  hyperbolic_curvature: 1.0
  learnable_curvature: true
  use_raw_residual: true
  use_complex_residual: false
  use_hyper_relation: true
  use_gated_raw_residual: true
  use_gated_complex_residual: false
  local_fusion_type: add
  relation_condition_type: residual
  relation_gate_scale: 0.1
  relation_film_scale: 0.1
  mask_mode: binomial
  downstream_mask: all_true

  classifier_head:
    head_type: avg_pool
    hidden_dims: [128]
    dropout: 0.3
    attn_n_head: 4
    attn_head_dim: 64
    fusion_mode: dual
    fusion_n_head: 4
    fusion_head_dim: 64
    fusion_use_rope: true
    fusion_rope_theta: 10000.0
    fusion_max_seq_len: 1024

training:
  max_epochs: 50
  weight_decay: 0.01
  max_grad_norm: 3.0

  lr_schedule: onecycle
  max_lr: 5e-4
  encoder_lr_scale: 0.1
  warmup_epochs: 5
  warmup_scale: 1e-2
  pct_start: 0.2
  min_lr: 1e-6

  freeze_encoder: true
  use_amp: false
  label_smoothing: 0.0

  lora:
    use_lora: false
    lora_r: 8
    lora_alpha: 16
    lora_dropout: 0.0
    lora_target_modules: [default]
    lora_exclude_modules: null
    lora_target_type: default
    lora_scope: transformer
    lora_lr_scale: 1.0

logging:
  experiment_name: relation_cgeom_bcic2a_physioMI_pretrained_freeze_encoder
  run_dir: assets/run
  run_group: bcic2a_physioMI_pretrained_freeze_encoder
  use_cloud: false
  cloud_backend: wandb
  project: relation_cgeom
  entity: null
  api_key: null
  offline: true
  tags: [relation_cgeom, bcic_2a, physioMI_pretrained, freeze_encoder]
  log_step_interval: 10
  ckpt_interval: 1
  save_best_only: true
  save_last_checkpoint: true
  monitor_split: eval
  monitor_metric: balanced_acc
  monitor_mode: max
